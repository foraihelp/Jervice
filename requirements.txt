anthropic>=0.39.0
openai>=1.50.0   # used when config.yaml's brain.provider is "openai" (also covers any OpenAI-compatible endpoint)
python-dotenv>=1.0.1
PyYAML>=6.0.1
ruamel.yaml>=0.18.6   # used only for settings-window saves, to preserve config.yaml's comments

# Desktop UI window (embeds the app's HTML/CSS/JS in a native window via
# Windows' built-in WebView2 runtime -- see README for the WebView2 requirement)
pywebview>=5.3

# Local API server (lets the iOS app / other devices talk to this brain)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Audio capture / playback
# (silence detection is done with plain numpy RMS -- no compiled VAD
# library needed, to avoid Windows wheel-build failures on newer Python versions)
sounddevice>=0.4.7
numpy>=1.26.4

# Wake word (fully local, free)
openwakeword>=0.6.0

# Speech-to-text (fully local, free)
faster-whisper>=1.0.3

# Text-to-speech (fully local, uses Windows SAPI5 voices)
pyttsx3>=2.90

# System tray icon
pystray>=0.19.5
Pillow>=10.4.0

# Windows automation
pywin32>=306; sys_platform == "win32"
pygetwindow>=0.0.9
psutil>=6.0.0
pycaw>=20240210; sys_platform == "win32"   # volume control
comtypes>=1.4.5; sys_platform == "win32"   # required by pycaw
